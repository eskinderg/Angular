export enum TooltipPosition {
    ABOVE = 'above',
    BELOW = 'below',
    LEFT = 'left',
    RIGHT = 'right',
    DYNAMIC = 'dynamic',
    DEFAULT = TooltipPosition.DYNAMIC
}

export enum TooltipTheme {
    DARK = 'dark',
    LIGHT = 'light',
    DEFAULT = TooltipTheme.DARK
}
