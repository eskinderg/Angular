#.travis.yml

language: node_js
node_js:
  - "6.10.0"

branches:
  only:
    - dev

before_script:
  - npm install -g @angular/cli
  - npm install

script:
  - ng build

after_success:

notifications:
  email:
    on_failure: change
    on_success: change
